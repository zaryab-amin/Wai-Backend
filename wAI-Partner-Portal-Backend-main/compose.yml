services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: backend
    container_name: wai-backend
    ports:
      - "8082:8081"
    env_file:
      - .env
    networks:
      - default
      - shared-network

networks:
  shared-network:
    external: true
